{
  "command": "show ip route",
  "level0": [
    {
      "pattern": "^\\s+(?P<supernet_prefix>(?:\\d{1,3}\\.){3}\\d{1,3})\\/(?P<supernet_prefix_length>\\d+).*?(?P<subnet_count>\\d+) subnets(?:, (?P<masks_count>\\d+) masks)?\\n(?P<prefixes>(?:^.*?(?:$\\s)?)+?(?=^.*?subnetted|^\\s+$|$))",
      "flags": 40
    }
  ]
}